.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
.DS_Store
lexio.db
*.db
extension.zip
*.zip
# …but keep the published Chrome extension that the site serves:
!static/lexio-extension.zip
.claude/

# iOS app — XcodeGen generates the project from ios/project.yml, so don't
# commit the project file or Xcode's build artifacts.
ios/*.xcodeproj
ios/*.xcworkspace
ios/build/
ios/DerivedData/
**/xcuserdata/
**/*.xcuserstate
.swiftpm/
static/*.dmg

# Apple signing material — never committed, obtained per-developer from
# App Store Connect (see desktop/APP_STORE.md).
desktop/build/*.provisionprofile
